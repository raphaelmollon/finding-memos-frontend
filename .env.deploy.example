# FTP Deployment Configuration
# Copy this file to .env.deploy and fill in your actual credentials
# NEVER commit .env.deploy to git!

# FTP Server Details
FTP_HOST=your-ftp-server.com
FTP_USER=your-ftp-username
FTP_PASSWORD=your-ftp-password
FTP_PORT=21

# Use secure FTP (FTPS)?
FTP_SECURE=false

# Remote path where frontend is deployed (without trailing slash)
# Example: /finding-memos or /public_html/finding-memos
FTP_REMOTE_PATH=/finding-memos
